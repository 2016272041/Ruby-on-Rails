<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @quiz.title %>
</p>

<p>
  <strong>Desc:</strong>
  <%= @quiz.desc %>
</p>

<%= link_to 'Edit', edit_quiz_path(@quiz) %> |
<%= link_to 'Back', quizzes_path %>

<br>

<div class="row">
  <h3><%= @quiz.title %></h3>
  <span class="right flow-text"> Current Score <%= @cur_score %></span>
</div>
<div class="row">
  <div class=card>
    <form method="POST" action="<%= @id %>">
      <div class="card-content">
        <p class="flow-text">
      </div>
    </form>
  </div>
</div>